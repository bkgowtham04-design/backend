// Authentication context - Login, Register, Logout, LocalStorage sync
import { createContext } from 'react';

export const AuthContext = createContext();

export default function AuthContextProvider() {
  return null;
}
